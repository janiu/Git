git diff HEAD -> sprawdzanie wprowadoznych zmian w respozytorium
git add Plik.txt  ->dodanie pliku o nazwie "Plik.txt" do repozytorium
git commit -m "Zmiana nr.5" -> zatwierdzenie zmian wprowadzonych w repozytorium pod nazwa "Zmiana nr.5"
git status -> pokazuje status plikow gotowych do commita
git log-> pokazuje liste zmian wprowadoznych w tym repozytorium
q -> wychodzi z log-ów
git branch cleanUp -> utowrzenie gałezi o nazwie cleanUp
git checkout cleanUp -> przelaczenie sie na gałez cleanUp
git merge cleanUp ->połaczeni zmian w dwoch gałeziach (cleanUp i tej w ktorej jestesmy)
git branch -d cleanUp ->usuniecie gałezi o nazwie "cleanUp"
